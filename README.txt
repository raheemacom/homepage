# Raheema Communication Website

Open `index.html` in a browser.

Files:
- index.html — page structure/content
- style.css — responsive styling
- script.js — mobile menu, active navigation, service search, quote modal, scroll animations

The layout is recreated from the supplied reference image. Replace the CSS-generated portfolio/hero artwork with your own product photos when available.
